import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#f44336',
            light: '#e33329'
        },
        secondary: {
            main: '#3f51b5',
        },
        otherColor: {
            main: '#ff9800'
        }
    }
})